# Отчёт по результатам анализа Kubernetes Audit Log

## Подозрительные события

1. Доступ к секретам:
    - Кто: kubernetes-admin
    - Где: "namespace": "kube-system", "name": "bootstrap-token-aomg6w",
    - Почему подозрительно: kubernetes-admin кто-такой? cluster-admin ему можно

2. Привилегированные поды:
    - Кто: minikube-user
    - Комментарий: запущен под  "securityContext": {      "privileged": true      } 

3. Использование kubectl exec в чужом поде:
    - Кто: что то нету
    - Что делал: скорее всего отсеклось выполнение команды из симуляции

4. Создание RoleBinding с правами cluster-admin:
    - Кто: RoleBinding escalate-binding не обнаружено (по поиску по objectRef.resource=="rolebindings" и имени escalate-binding).
    - К чему привело: вероятно миникуб сначала сделал привязки по политике, а после включил логирование

5. Удаление audit-policy.yaml:
    - Кто: не выявлено, скрипт симуляции не там удаляет (Наставник подтвердил)
    - Возможные последствия: 

## Вывод

С зафиксировали логи действий с секретом, привилигированными подами
Такое может привести к утечке
Нужно 
   - ограничить права и действия cluster-admin, в этом в предыдущих эта роль не создавалась так как по умолчанию создается
   - вести постоянно адит-лог